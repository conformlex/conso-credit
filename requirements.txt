# Core pipeline (database, generation, export) needs the standard library only.
# Everything below is for training, the API and the console.
pandas>=2.0
scikit-learn>=1.4
joblib>=1.3
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
